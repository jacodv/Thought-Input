{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CapturePayload",
  "description": "Payload sent by Brain Input clients when a capture is submitted.",
  "type": "object",
  "required": ["text", "timestamp", "source_platform", "client_version", "capture_method", "idempotency_key"],
  "properties": {
    "text": {
      "type": "string",
      "minLength": 1,
      "description": "The captured text content."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when the capture was initiated."
    },
    "source_platform": {
      "type": "string",
      "enum": ["macos", "android"],
      "description": "Platform that originated the capture."
    },
    "client_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version of the client app."
    },
    "capture_method": {
      "type": "string",
      "enum": ["typed", "voice"],
      "description": "How the text was entered."
    },
    "idempotency_key": {
      "type": "string",
      "format": "uuid",
      "description": "UUID for deduplication on the server side."
    }
  },
  "additionalProperties": false
}
